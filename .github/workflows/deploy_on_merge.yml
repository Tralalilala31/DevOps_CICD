name: Deploy on merge
on:
  push:
    branches:
      - main
      - develop
      - cicd # TEMPORAIRE, supprimer

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "placeholder"
