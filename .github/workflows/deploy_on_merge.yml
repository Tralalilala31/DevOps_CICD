name: Deploy on merge
on:
  push:
    branches:
      - main
      - develop
      - feature/cicd # TEMPORAIRE, supprimer
  workflow_dispatch:

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "placeholder"
